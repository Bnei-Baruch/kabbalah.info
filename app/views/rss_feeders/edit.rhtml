<h1>Editing rss_feeder</h1>

<%= render :partial => 'form',
  :locals => {
    :path => rss_feeder_path(@rss_feeder),
    :method => :put,
    :button_text => 'Update'
  } %>

<%= link_to 'Cancel', session[:referer] %>

